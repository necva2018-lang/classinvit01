# 複製為 .env 後填入真實值（勿將 .env 提交到 Git）

# Zeabur PostgreSQL：在服務面板複製連線字串，貼到 Zeabur Web Service 與本機 .env
# 僅在伺服器／建置時使用；Next.js 不會把此變數暴露給瀏覽器 bundle（除非誤用 NEXT_PUBLIC_ 前綴）
DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public"
