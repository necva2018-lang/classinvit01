# 複製為 .env 後填入真實值（.env 已被 .gitignore，勿提交到 Git）

# Zeabur PostgreSQL 常見格式（外連、需 SSL）：
# 將 root / 密碼 / 主機 / 埠號 換成你在 Zeabur 面板「連線資訊」看到的內容。
# 若密碼含 @ # 等特殊字元，請先 URL 編碼後再填入。
#
# DATABASE_URL="postgresql://root:你的密碼@主機位址:埠號/zeabur?sslmode=require"

# 泛用寫法（同上，自行替換）：
DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DATABASE?sslmode=require"

# 說明：
# - 僅在伺服器／本機後端使用；不要加 NEXT_PUBLIC_ 前綴。
# - Zeabur Web Service 請在控制台「環境變數」新增同名 DATABASE_URL，值與本機 .env 相同即可。
